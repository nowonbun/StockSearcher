# AGENTS.md

## 목적
- 이 저장소에서 작업하는 에이전트의 기본 행동 규칙을 정의한다.
- 대상 프로젝트는 Python 기반 주식 데이터 수집 / 학습 / 추론 / MCP 서버 프로젝트다.

## 우선순위
1. 정확성
2. 재현 가능성
3. 운영 안정성
4. 최소 변경

## 작업 원칙
- 답변과 작업 보고는 항상 한국어로 한다.
- 결론을 먼저 말하고, 이유와 근거를 뒤에 적는다.
- 추측을 사실처럼 말하지 않는다.
- 사용자가 틀린 전제를 주면 명확히 지적한다.
- 변경은 필요한 범위만 최소화한다.
- 기존 파일 구조와 코딩 스타일을 먼저 따른다.

## 프로젝트 성격
- `dataset_jp.py`: 일본 시장 데이터 수집
- `dataset_kr.py`: 한국 시장 데이터 수집
- `model_jp.py`, `model_kr.py`: 모델 학습
- `predict_jp.py`, `predict_kr.py`: 예측 결과 생성
- `webapp.py`: UI + MCP 엔드포인트(`/mcp`)
- `ddl.sql`: DB 스키마 참고
- `config.ini`: 실행 환경 설정

## 기술 규칙
- Python 변경 시 관련 import, 실행 경로, 설정 파일 의존성을 함께 확인한다.
- DB 관련 변경 시 `ddl.sql`, 실제 쿼리, 컬럼명 일관성을 같이 본다.
- KR/JP 로직은 시장별 차이를 유지하고, 공통화는 명확한 이점이 있을 때만 한다.
- 학습/추론 코드 수정 시 입력 데이터 컬럼, 모델 파일(`model_*.pt`), 출력 포맷 호환성을 확인한다.
- MCP 관련 수정 시 `/mcp` 경로, 툴 이름, 파라미터 형식, 반환 형식을 깨지 않게 유지한다.

## MCP / 데이터 조회 규칙
- 주식/예측 데이터 확인이 필요하면 가능하면 Stock MCP를 우선 사용한다.
- 시장 코드는 `KR`, `JP`만 사용한다.
- `predict_rows` 호출 시 `market`, `as_of`를 반드시 확인한다.
- `stock_data` 호출 시 `market`, `code`를 반드시 확인한다.

## 파일 수정 규칙
- 수정 전 관련 파일을 먼저 읽고 구조를 파악한다.
- 큰 리팩터링보다 국소 수정 우선.
- 불필요한 rename, formatting-only 변경은 피한다.
- 비밀정보, 실제 비밀번호, 토큰은 파일에 하드코딩하지 않는다.

## 검증 규칙
- 코드 수정 후 가능한 최소 검증을 수행한다.
- 우선 검증 순서:
  1. 문법 오류 확인
  2. import/runtime 진입 오류 확인
  3. 영향 받은 기능만 국소 실행
- 네트워크/외부 DB 의존 작업은 실패 가능성을 명시한다.

## 문서 규칙
- 동작 방식이나 실행 방법이 바뀌면 `README.md`도 함께 갱신 여부를 확인한다.
- MCP 엔드포인트/파라미터가 바뀌면 관련 문서(`README.md`, `stock-mcp.md`)를 같이 본다.

## 금지 사항
- 근거 없이 모델 성능을 단정하지 않는다.
- 운영 데이터나 로그를 임의 삭제하지 않는다.
- 사용자가 요청하지 않은 파괴적 명령(`rm`, 대량 삭제, reset`)을 실행하지 않는다.
- 테스트되지 않은 대규모 구조 변경을 한 번에 적용하지 않는다.

## 권장 응답 형식
- 결론
- 변경/분석 근거
- 실행한 검증
- 남은 리스크
