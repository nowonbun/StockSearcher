# 변경 이력 - 2026-03-22

## 변경 대상 파일

| 파일 | 변경 내용 |
|------|----------|
| `model_jp.py` | 예측 성능과 학습 안정성 향상을 위한 구조 개선 |
| `model_kr.py` | `model_jp.py`와 동일한 개선 적용, KR 기본값 유지 |
| `predict_jp.py` | 학습 변경 사항에 맞춰 추론 파이프라인 수정 |
| `predict_kr.py` | `predict_jp.py`와 동일한 수정 적용, KR 기본값 유지 |

---

# model_jp.py 변경 이력

## 배경 및 목적

예측 성능 향상을 목표로 아래 문제를 수정 및 개선했다.

- `adaptive_pos_weight` 로직이 사실상 no-op 상태였음
- 글로벌 정규화로 인해 종목 간 가격 스케일 차이를 충분히 흡수하지 못함
- 불균형 데이터 환경에서 `val_loss` 기준 모델 저장은 실전 목적과 맞지 않음
- LSTM 마지막 hidden state만 사용해 시계열 정보 활용이 제한됨
- 학습 안정화 장치가 부족함
- RSI / MACD 같은 대표 파생 피처가 없음

## 변경 상세

### 1. `adaptive_pos_weight` 버그 수정

**대상:** `train_loop()` 내 epoch 루프

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 코드 | `current_weight = current_weight` (no-op) | 제거 |
| 방향 로직 | 조정 방향이 일관되지 않음 | precision 하락 시 weight 감소, recall 하락 시 weight 증가 |
| 하한값 | 없음 | `max(1.0, current_weight)` 적용 |

```python
if prec < prev_prec - metric_eps:
    current_weight *= (1.0 - pos_weight_step)
else:
    current_weight *= (1.0 + pos_weight_step)
current_weight = max(1.0, current_weight)
```

---

### 2. 윈도우별 z-score 정규화 적용

**대상:** `WindowIterableDataset.__iter__()`

- 기존 글로벌 mean/std 기반 정규화 제거
- 각 시퀀스 윈도우마다 평균/표준편차를 계산해 정규화
- 종목 간 절대 가격 차이에 덜 민감한 입력으로 변경

```python
x = features[i : i + self.seq_len].copy()
x_mean = x.mean(axis=0, keepdims=True)
x_std = x.std(axis=0, keepdims=True)
x = (x - x_mean) / (x_std + 1e-8)
```

함께 제거된 요소:

- `compute_feature_stats()` 함수
- `WindowIterableDataset`의 `mean`, `std` 인자
- `main()`의 mean/std 계산 로직

---

### 3. 파생 피처 추가

**대상:** `compute_rsi()`, `_ema()`, `compute_macd()`, `append_computed_features()`

추가 피처:

| 피처 | 계산 기준 | 설명 |
|------|-----------|------|
| `RSI_14` | Close | 과매수/과매도 구간 판단용 |
| `MACD` | Close | EMA(12) - EMA(26) 기반 추세 신호 |
| `MACD_signal` | MACD | EMA(9) 기반 신호선 |

- DB 스키마 변경 없이 코드에서 계산
- `FEATURE_COLS` 18개 + `COMPUTED_COLS` 3개 = `ALL_FEATURE_COLS` 21개
- 모델 입력 차원도 18 → 21로 변경

---

### 4. Attention Pooling 추가

**대상:** `PriceLSTM.forward()`

기존:

```python
last = out[:, -1, :]
return self.head(last).squeeze(-1)
```

변경:

```python
scores = self.attn(out)
weights = torch.softmax(scores, dim=1)
context = (out * weights).sum(dim=1)
return self.head(context).squeeze(-1)
```

추가 변경:

- `self.attn = nn.Linear(hidden_size, 1)` 추가
- head 구조를 `LayerNorm`, `Dropout` 포함 형태로 강화

---

### 5. `FocalLoss` 클래스 추가

**대상:** 신규 클래스 `FocalLoss`

- 극단적 클래스 불균형 환경에서 hard example에 집중
- `--use-focal-loss` 옵션으로 활성화
- `--focal-gamma` 기본값 2.0
- `pos_weight`와 병행 가능

---

### 6. Gradient Clipping 추가

**대상:** `train_loop()`의 `optimizer.step()` 직전

```python
if clip_grad_norm > 0:
    nn.utils.clip_grad_norm_(model.parameters(), clip_grad_norm)
```

- 신규 옵션: `--clip-grad-norm`
- 기본값: `1.0`
- `0`이면 비활성화

---

### 7. CosineAnnealingLR 스케줄러 추가

**대상:** `train_loop()`

```python
scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=epochs)
```

- epoch마다 `scheduler.step()` 호출
- 로그에 현재 learning rate 출력 추가

---

### 8. 모델 저장 기준을 F1로 변경

**대상:** `train_loop()` 저장 조건

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 저장 기준 | `val_loss` 최소 | `f1` 최대 |
| 이유 | 불균형 환경에서 실전 목적과 괴리 가능 | precision / recall 균형 반영 |

---

## 신규 CLI 옵션

| 옵션 | 기본값 | 설명 |
|------|--------|------|
| `--clip-grad-norm` | `1.0` | gradient clipping max norm |
| `--use-focal-loss` | `False` | Focal Loss 사용 여부 |
| `--focal-gamma` | `2.0` | Focal Loss gamma 값 |

---

## 영향 범위

- 변경 파일: `model_jp.py`
- 기존 `model_jp.pt` 체크포인트는 입력 차원 18 기준일 수 있어 재학습이 필요할 수 있음
- DB 스키마 변경은 없음

---

# model_kr.py 변경 이력

## 배경 및 목적

`model_jp.py`에 적용한 개선 사항을 KR 모델에도 동일하게 반영했다.
다만 KR 전용 기본값은 유지했다.

## 변경 상세

아래 항목이 `model_jp.py`와 동일하게 적용됐다.

1. `adaptive_pos_weight` 로직 수정
2. 윈도우별 z-score 정규화 적용
3. RSI / MACD / MACD_signal 파생 피처 추가
4. Attention Pooling 추가
5. `FocalLoss` 추가
6. Gradient Clipping 추가
7. CosineAnnealingLR 추가
8. 모델 저장 기준을 F1로 변경

## KR 전용 기본값

| 항목 | JP 값 | KR 값 |
|------|-------|------|
| DB config | `db_config_jp` | `db_config_kr` |
| `--table` 기본값 | `STOCK_DATA_JP` | `STOCK_DATA_KR` |
| `--model-out` 기본값 | `model_jp.pt` | `model_kr.pt` |
| `--pos-weight` | `33.77` | `17.6` |
| `--pos-weight-step` | `0.05` | `0.01` |
| `--min-trans-amnt-sum` | `500000000 * 5` | `5000000000 * 5` |
| `--eval-threshold` | `0.5` | `0.60` |

## 영향 범위

- 변경 파일: `model_kr.py`
- 기존 `model_kr.pt` 체크포인트는 입력 차원 18 기준일 수 있어 재학습이 필요할 수 있음
- DB 스키마 변경은 없음

---

# predict_jp.py 변경 이력

## 배경 및 목적

`model_jp.py`의 입력 피처, 정규화 방식, 입력 차원 변경에 맞춰
추론 파이프라인도 학습과 동일한 사양으로 맞췄다.

## 변경 상세

### 1. import 수정

- `compute_feature_stats` 제거
- `ALL_FEATURE_COLS` 추가
- `append_computed_features` 추가
- `CLOSE_INDEX` 추가

### 2. `_fetch_sequence()`에 파생 피처 추가

기존:

```python
rows = rows[::-1]
return np.array(rows, dtype=np.float32)
```

변경:

```python
rows = rows[::-1]
features = np.array(rows, dtype=np.float32)
closes = features[:, CLOSE_INDEX]
return append_computed_features(features, closes)
```

### 3. `predict_probs()` 정규화 방식 변경

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 입력 인자 | `mean`, `std` 필요 | 제거 |
| 정규화 방식 | 글로벌 mean/std | 윈도우별 z-score |

```python
x = seq.copy()
x_mean = x.mean(axis=0, keepdims=True)
x_std = x.std(axis=0, keepdims=True)
x = (x - x_mean) / (x_std + 1e-8)
x_t = torch.from_numpy(x[None, ...])
```

### 4. `main()` 수정

- `compute_feature_stats()` 호출 제거
- `predict_probs()` 호출에서 `mean`, `std` 제거
- `PriceLSTM(input_size)`를 `len(ALL_FEATURE_COLS)`로 변경

## 영향 범위

- 변경 파일: `predict_jp.py`
- 학습/추론 입력 스펙이 일치하도록 수정
- DB 스키마 변경은 없음

---

# predict_kr.py 변경 이력

## 배경 및 목적

`predict_jp.py`에 적용한 수정 사항을 KR 추론 코드에도 동일하게 반영했다.
다만 KR 전용 기본값은 유지했다.

## 변경 상세

아래 항목이 `predict_jp.py`와 동일하게 적용됐다.

1. import 수정
2. `_fetch_sequence()`에 파생 피처 추가
3. `predict_probs()`에서 윈도우별 z-score 정규화 적용
4. `main()`에서 글로벌 mean/std 의존 제거 및 입력 차원 21 반영

## KR 전용 기본값

| 항목 | JP 값 | KR 값 |
|------|-------|------|
| DB config | `db_config_jp` | `db_config_kr` |
| `--table` 기본값 | `STOCK_DATA_JP` | `STOCK_DATA_KR` |
| `--model` 기본값 | `model_jp.pt` | `model_kr.pt` |
| `--min-trans-amnt-sum` | `200000000 * 5` | `5000000000 * 5` |
| DB 저장 테이블 | `STOCK_PREDICT_JP` | `STOCK_PREDICT_KR` |

## 영향 범위

- 변경 파일: `predict_kr.py`
- 학습/추론 입력 스펙이 일치하도록 수정
- DB 스키마 변경은 없음
